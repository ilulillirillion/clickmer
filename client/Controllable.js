// vim: set ft=javascript:

/**
 * file: clickmer/client/Controllable.js
 * author: zolvaring
 * email: zolvaring@gmail.com
 * reference: https://github.com/zolvaring/clickmer
 **
 * Provides the Controllable mixin.
 * Extends an object so that it can be controlled by the client.
 */

const Controllable = Base => class extends Base {

  /**
   * Extends a base with the controllable mixin.
   * Provides a delta value for cartesian coordinates, so that the client can
   * track 
   */
  constructor(...args) {

    super(...args);


  }
